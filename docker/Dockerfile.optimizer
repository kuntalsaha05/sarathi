FROM python:3.11-slim

WORKDIR /app

COPY core-engine/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY core-engine/ ./core-engine/

EXPOSE 8000

CMD ["uvicorn", "core-engine.app.main:app", "--host", "0.0.0.0", "--port", "8000"]
